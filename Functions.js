//An arrow function in JavaScript is a shorter way to write a function using the => (arrow) syntax. It was introduced in ES6.

const functionName = (parameters) => {
    // code
};

const add = (a, b) => {
    return a + b;
};
console.log(add(2, 3)); // 5

const square = x => x * x;

//A function is a reusable block of code that performs a specific task. It can take inputs (parameters) 
// and return an output.

function functionName(parameters) {
    // code
    return result;
}

function add(a, b) {
    return a + b;
}
console.log(add(2, 3)); // 5